# Python 3.10+
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
torch>=2.2          # CPU build is enough: pip install torch --index-url https://download.pytorch.org/whl/cpu
onnx>=1.15
onnxruntime>=1.17
onnxscript>=0.1
matplotlib>=3.7
pytest>=7
cryptography>=41     # web server: generates its self-signed TLS certificate on first run
