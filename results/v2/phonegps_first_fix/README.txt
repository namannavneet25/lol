Phone-GPS runs after the FIRST sparse-GNSS fix (plausibility bound 60 m/s), before the reviewer-requested
filter-speed bound. The original pre-fix runs (drift in the thousands of %) were overwritten by mistake.
